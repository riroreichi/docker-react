language: generic 
sudo: required
services:
  - docker

before_install:
  - docker build -t riroreichi/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true riroreichi/docker-react npm run test